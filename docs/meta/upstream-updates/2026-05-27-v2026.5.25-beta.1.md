# Upstream Tracking — 2026.5.25-beta.1

- **Version**: 2026.5.25-beta.1
- **Release date**: 2026-05-26
- **Type**: beta (prerelease)
- **Tracked**: 2026-05-27
- **Previous beta**: 2026.5.24-beta.2

## Operator-relevant changes

### Alpine Linux support
- CLI installer uses Alpine's `apk` Node.js, npm, Git packages on musl Linux
- Detect musl Linux shells (Alpine) as Linux instead of rejecting before npm install

### Performance
- Cache manifest-backed CLI provider descriptors and fallback provider resolution
- Prefilter tracked conflict-marker scans so changed checks avoid reading every file on clean runs

### Config & Secrets
- Allow exec SecretRef ids to include `#` selectors (AWS-style `secret#json_key`)
- Config/doctor: narrow profiled tool-section doctor repair, warn on unreadable cron stores

### Channels
- iMessage: dedupe watcher startup when accounts list both `default` and same named source
- iMessage: thread attachment roots into image tool for `~/Library/Messages/Attachments`

### Agents
- Codex: preserve sandbox bootstrap path style across host/sandbox remapping
- Fail closed when provider-less session models match multiple provider-prefixed runtime policies
- Subagents: avoid loading bundled channel plugins during completion delivery policy resolution

### Docs
- Model-usage portability, Codex migration prerequisites, thread-bound subagent limits
- Browser CDP diagnostics, Plugin SDK allowlist imports, cron HEARTBEAT handling
- GLM consolidated under Z.AI, Upstash Box install guide, Gateway exposure runbook

### Models
- OpenRouter: use endpoint-specific context limits from `top_provider` metadata (no more overstated context)

### Windows
- Cross-platform launcher for direct Node package scripts
- Vitest import timing, test runners, Docker E2E harness all work on native Windows

### Control UI
- Collapsed tool rows more readable, raw expanded tool details preserved
- Post-compaction AGENTS.md reinjection opt-in (avoids duplicated project context)
- Split large build-time runtime dependencies into stable chunks (Linux/Docker install stays under chunk warning)

## Fixes of note

- Installer: avoid incompatible `--before` filter when raw npm `min-release-age` config present
- MCP: bound bundled MCP `tools/list` catalog discovery so hung servers don't block tool materialization
- Auth/Codex: actionable `log.warn` for legacy Codex OAuth sidecar loader when seed is only in macOS Keychain

## Local install status

- Local CLI: 2026.5.7 (still behind)
