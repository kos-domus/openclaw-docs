# Upstream Tracking — 2026.5.24-beta.2

- **Version**: 2026.5.24-beta.2
- **Release date**: 2026-05-24
- **Type**: beta (prerelease)
- **Tracked**: 2026-05-27
- **Previous stable**: 2026.5.22

## Operator-relevant changes

### Performance & Gateway
- Gateway caches stable install-record, channel-catalog, bundled-channel, plugin metadata, Telegram session-store on hot paths
- Lazy-load startup-idle plugin work and core gateway method handlers
- Cache plugin SDK public-surface alias maps, skip irrelevant macOS Linuxbrew PATH probes
- Pre-warm provider auth-state map at startup: `/models` drops from ~20s to ~5ms (~4,100×)

### Channels
- iMessage thumb-approval reactions (👍/👎) without textual `/approve`
- Discord voice: realtime wake-name gating, longer SOUL.md bootstrap budget
- Telegram: typing/progress context preserved, forum topic names, entity preservation
- WhatsApp: thumb approval reactions, deferred media reply fix
- Signal: reaction approvals

### Voice & Talk
- WebUI and Discord voice: inspect/steer/cancel active consults during Talk
- Shared realtime turn-context tracking, output activity tracking, consult question matching

### Agents & Subagents
- Default sub-agent bootstrap context limited to AGENTS.md + TOOLS.md (persona/memory excluded)
- Named model login profiles with auth migration for Hermes/OpenCode/Codex
- Adaptive image compression with `agents.defaults.imageQuality` preference
- Session lock max-hold reclaim, overflow budgets from provider errors
- OpenAI sampling params forwarded through Gateway, estimated context-budget status

### New features
- Meeting Notes: source-only external plugin with transcript capture, Discord voice as first live source
- `cron.maxConcurrentRuns` defaults to 8 for parallel scheduled automations
- Activity tab in Control UI for sanitized live tool activity summaries
- `qa coverage --match <query>` for focused proof selection
- Plugin SDK: generic channel-message poll sender, row-level session workflow helpers
- `embeddingProviders` capability contract for reusable provider surface

### Docs
- Dense channel/config documentation hardening (Signal configPath, Telegram wildcard topics, SecretRef guidance, etc.)
- Cron output language prompts, skill advanced features, gateway upstream 403 troubleshooting
- Context-pruning ratio bounds, local dashboard recovery, Bitwarden SecretRef setup

### Security
- Session allowlist: replace wildcard regexes with precompiled linear matcher (no backtracking)
- Escape transcript metadata field names
- Require owner identity proof before stale plugin lock removal
- Validate plugin model-pattern regexes safely
- Audit Claude permission overrides under YOLO

### Packaging
- Exclude documentation images/assets from npm tarball (smaller package)
- Shrinkwrap for npm root package and OpenClaw-owned plugins

## Fixes of note

- Telegram `ENETDOWN` treated as transient pre-connect failure
- Telegram `/command@TargetBot` entities handled as explicit mentions
- Codex: preserve native web-search metadata, sandbox bootstrap path style, yolo approval policy
- MiniMax OAuth token expiry stored as absolute ms timestamp (no false expiration)
- Anthropic: strip missing/blank thinking signatures for signed-thinking providers
- Gateway restart: honor configured restart drain budget, avoid double deferral timeout
- Config/SecretRef: preflight active runtime SecretRefs before writes, rollback on failure
- Memory: prevent semantic vector index degradation when embeddings unavailable, CJK-aware dreaming dedupe

## Local install status

- Local CLI: 2026.5.7
- Latest stable: 2026.5.22
- This beta: 2026.5.24-beta.2
- Gap: local is 15 releases behind stable; significant operator-relevant changes accumulating
